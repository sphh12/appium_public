# GME Appium Test - Environment Variables Template
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.
# cp .env.example .env (또는 PowerShell: Copy-Item .env.example .env)

# ===========================================
# 테스트 계정 정보 (필수)
# ===========================================
# 테스트에 사용할 사용자 ID
GME_TEST_USERNAME=your_test_username

# 테스트에 사용할 PIN 번호
GME_TEST_PIN=your_pin

# ===========================================
# 앱 설정 (필수)
# ===========================================
# 앱 패키지의 resource-id 접두사
# 예: com.example.app:id, com.myapp.android:id
GME_RESOURCE_ID_PREFIX=com.example.app:id

# APK 파일명 (apk/ 폴더 내 파일)
# 예: MyApp_1.0.0.apk
GME_APK_FILENAME=app.apk

# ===========================================
# 디바이스 설정 (선택사항)
# ===========================================
# 실물 디바이스 시리얼 (adb devices로 확인)
# 에뮬레이터 사용 시 주석 처리
# ANDROID_UDID=XXXXXXXX

# 디바이스 이름 (Allure 리포트 표시용)
# ANDROID_DEVICE_NAME=My Device

# Android OS 버전
# ANDROID_PLATFORM_VERSION=12

# ===========================================
# Appium 서버 설정 (선택사항)
# ===========================================
# Appium 서버 호스트 (기본: 127.0.0.1)
# APPIUM_HOST=127.0.0.1

# Appium 서버 포트 (기본: 4723)
# APPIUM_PORT=4723

# ===========================================
# iOS 설정 (macOS에서만 사용)
# ===========================================
# iOS 시뮬레이터 이름
# IOS_DEVICE_NAME=iPhone 15

# iOS 버전
# IOS_PLATFORM_VERSION=17.0

# 앱 번들 ID
# IOS_BUNDLE_ID=com.example.app
